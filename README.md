My first try using React components.

A very simple React app that renders a component which provides an interface for users to check the temperature in a given area, using the Weather Underground API.
